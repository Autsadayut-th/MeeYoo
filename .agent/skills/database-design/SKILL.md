# Database Design
Use clear naming, explicit relationships, appropriate indexes, and data integrity constraints.
Avoid N+1 and unbounded queries.
Select only required fields and paginate large datasets.
Before schema changes, inspect relationships, indexes, migrations, and existing queries.
Never expose sensitive database fields through API responses.
