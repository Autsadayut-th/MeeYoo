# React Best Practices
Build maintainable production React applications.
Use small reusable components and composition. Keep API communication in services/hooks.
Avoid unnecessary state and useEffect. Handle Loading, Success, Empty, and Error states.
Use semantic HTML, keyboard support, labels, focus states, and accessible interactions.
Use memoization/lazy loading/code splitting only when justified.
