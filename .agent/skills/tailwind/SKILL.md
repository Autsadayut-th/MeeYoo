# Tailwind CSS Skill
Use consistent spacing, typography, radius, shadows, and color tokens.
Avoid excessive arbitrary values.
Build responsive layouts intentionally.
Support dark mode with sufficient contrast when enabled.
Extract repeated patterns into reusable components.
Do not introduce new styles when an existing design pattern exists.
