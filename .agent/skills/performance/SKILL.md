# Performance Skill
Optimize based on evidence.
Check React re-renders, bundle size, expensive calculations, duplicate requests, API payloads, database queries, N+1 queries, and pagination.
Use caching only with an invalidation strategy.
Measure before and after optimization.
