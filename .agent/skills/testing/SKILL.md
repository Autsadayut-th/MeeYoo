# Testing Strategy
Test important behavior with appropriate unit, integration, component, API, and E2E tests.
Cover success, failure, validation, authentication, authorization, and edge cases.
Tests should be deterministic and independent.
Test behavior rather than implementation details.
Run relevant tests and build checks before completion.
