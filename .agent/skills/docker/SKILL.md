# Docker Skill
Use reproducible, secure container environments.
Prefer small base images, multi-stage builds where useful, non-root users, environment variables, and .dockerignore.
Never include secrets or unnecessary development dependencies in production images.
Use health checks where appropriate.
