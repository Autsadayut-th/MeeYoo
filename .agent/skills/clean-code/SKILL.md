# Clean Code Skill
Prefer readable, maintainable code over clever code.
Use descriptive names, small focused functions/components, avoid duplication and magic values.
Reuse existing utilities before creating new ones.
Keep business logic out of presentation components and controllers.
Remove unused imports, variables, and dead code.
